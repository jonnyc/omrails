<%= simple_form_for(@pin, html: {class: "form-horizontal"}) do |f| %>
  <%= f.error_notification %> <!-- Notification system -->

  <!-- Notification system for paperclip -->
  <%= f.full_error :image_file_size, class: "aler alert-error" %>
  <%= f.full_error :image_content_type, class: "aler alert-error" %>


  <%= f.input :image, label: "Upload an image" %>

  <!-- <div class="form-inputs"> --> <!-- don't need this div for some reason -->
    <%= f.input :description, as: :text, input_html: {rows: "3"} %>
<!--   </div> -->

  <div class="form-actions">
    <%= f.button :submit, class: "btn btn-primary" %>
  </div>
<% end %>
